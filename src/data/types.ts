export interface Project {
  title: string;
  description: string;
  url: string;
  source?: string;
}

export interface ReadingItem {
  title: string;
  url: string;
  source?: string;
  note?: string;
}
